# synchealth frontend — environment variables example.
# Copy this file to `.env` for local dev.
# Real `.env` is gitignored; never commit secrets.

# Where the API lives when running outside Docker (Vite proxy).
VITE_API_TARGET=http://localhost:8790

# Path prefix the frontend uses to reach the backend (defaults to /api).
VITE_API_PREFIX=/api